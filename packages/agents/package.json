{
  "name": "@cabal/agents",
  "version": "0.1.0",
  "private": true,
  "description": "Claude powered Cabal players: narrative prompts, structured decisions, legal order validation, and a WebSocket client",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "oxlint --deny-warnings src",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "oxlint": "^1.83.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0",
    "@types/node": "^24.6.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.126.0",
    "@cabal/engine": "workspace:*",
    "@cabal/protocol": "workspace:*",
    "zod": "^4.6.5"
  }
}
