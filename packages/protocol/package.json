{
  "name": "@cabal/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Zod schemas for the Cabal WebSocket and HTTP protocol",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "oxlint --deny-warnings src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "oxlint": "^1.83.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
